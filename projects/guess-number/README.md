# Guess the Number

Simple console game where the program chooses a random number between 1 and 100 and the player guesses it.

How to build:

clang++ main.cpp -o guess-number

How to run:

./guess-number

Notes:
- Uses <random> for better randomness. Handles non-integer input gracefully.
