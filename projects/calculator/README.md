# Simple Calculator

Small REPL-style calculator that supports addition, subtraction, multiplication and division on two numbers.

How to build:

clang++ main.cpp -o calculator

How to run:

./calculator

Notes:
- Enter expressions like `3 + 4` or `2.5 * 8`.
- Type `q` to quit when prompted for the first number.
